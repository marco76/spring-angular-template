spring:
  datasource:
    url: jdbc:h2:mem:example-app;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true

app:
  # Demo credentials for local development only; see docs/KNOWN_LIMITATIONS.md.
  bootstrap-admin:
    username: admin
    password: admin

